<nav>
    <a href="{{ path('index') }}">Home</a>

    {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('dashboard', {role: 'admin'}) }}">Admin</a>
    {% endif %}

    {% if is_granted('ROLE_COMERCIAL') %}
        <a href="{{ path('dashboard', {role: 'comercial'}) }}">Comercial</a>
    {% endif %}

    {% if is_granted('ROLE_JEFEPROYECTO') %}
        <a href="{{ path('dashboard', {role: 'jefeproyecto'}) }}">Jefe Proyecto</a>
    {% endif %}

    {% if is_granted('ROLE_EMPLEADO') %}
        <a href="{{ path('dashboard', {role: 'empleado'}) }}">Empleado</a>
    {% endif %}

    {% if app.user %}
        <a href="{{ path('app_logout') }}">Logout</a>
    {% else %}
        <a href="{{ path('app_login') }}">Login</a>
    {% endif %}
</nav>