<nav class="navbar navbar-expand-lg bg-dark">
    <a class="navbar-brand brand" href="{{ path('index') }}">Bugdet Maker</a>

    <div class="collapse navbar-collapse">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_user_index')}}">Usuarios</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_COMERCIAL') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_presupuesto_index') }}">Panel Comercial</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_JEFEPROYECTO') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_proyecto_index') }}">Proyectos</a>
                </li>
            {% endif %}

            {% if is_granted('ROLE_EMPLEADO') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_tarea_index') }}">Tareas</a>
                </li>
            {% endif %}
        </ul>

        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>
